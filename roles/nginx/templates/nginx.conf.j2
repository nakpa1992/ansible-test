server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root {{ web_root }};
    index index.html;

    server_name _;

    location / {
        try_files $uri $uri/ =404;
    }
}

